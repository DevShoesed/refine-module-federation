{
  "name": "refine-module-federation",
  "version": "1.0.0",
  "description": "Refine.dev microfontend module federation",
  "main": "index.js",
  "scripts": {
    "install:deps": "npm --prefix ./host install && npm --prefix ./mfe1 install",
    "preview:host": "npm --prefix ./host run build && npm --prefix ./host run preview",
    "preview:mfe1": "npm --prefix ./mfe1 run build && npm --prefix ./mfe1 run preview",
    "preview": "run-p preview:*"
  },
  "author": "Francesco Scarpato",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
